# generated by datamodel-codegen:
#   filename:  patched.schema.json

from __future__ import annotations

from typing import Annotated, Any, Literal

from pydantic import Field, RootModel

from pi_agent_events._base import AllowExtra


class QueueUpdateEvent(AllowExtra):
    """
    Session-specific events that extend the core AgentEvent
    """

    followUp: list[str]
    steering: list[str]
    type: Literal['queue_update']


class CompactionStartEvent(AllowExtra):
    """
    Session-specific events that extend the core AgentEvent
    """

    reason: Literal['manual', 'threshold', 'overflow']
    type: Literal['compaction_start']


class SessionInfoChangedEvent(AllowExtra):
    """
    Session-specific events that extend the core AgentEvent
    """

    name: str | None = None
    type: Literal['session_info_changed']


class AutoRetryStartEvent(AllowExtra):
    """
    Session-specific events that extend the core AgentEvent
    """

    attempt: float
    delayMs: float
    errorMessage: str
    maxAttempts: float
    type: Literal['auto_retry_start']


class AutoRetryEndEvent(AllowExtra):
    """
    Session-specific events that extend the core AgentEvent
    """

    attempt: float
    finalError: str | None = None
    success: bool
    type: Literal['auto_retry_end']


class CompactionResult(AllowExtra):
    """
    Result from compact() - SessionManager adds uuid/parentUuid when saving
    """

    details: Annotated[
        Any | None,
        Field(
            description='Extension-specific data (e.g., ArtifactIndex, version markers for structured compaction)'
        ),
    ] = None
    firstKeptEntryId: str
    summary: str
    tokensBefore: float


class DiagnosticErrorInfo(AllowExtra):
    code: str | float | None = None
    message: str
    name: str | None = None
    stack: str | None = None


class ExtractStopReasonAbortedError(RootModel[Literal['error', 'aborted']]):
    root: Literal['error', 'aborted']


class ExtractStopReasonStopLengthToolUse(
    RootModel[Literal['stop', 'length', 'toolUse']]
):
    root: Literal['stop', 'length', 'toolUse']


class ImageContent(AllowExtra):
    data: str
    mimeType: str
    type: Literal['image']


class RecordStringAny(AllowExtra):
    pass


class RecordStringUnknown(RootModel[dict[str, Any]]):
    root: dict[str, Any]


class TextContent(AllowExtra):
    text: str
    textSignature: str | None = None
    type: Literal['text']


class ThinkingContent(AllowExtra):
    redacted: Annotated[
        bool | None,
        Field(
            description='When true, the thinking content was redacted by safety filters. The opaque encrypted payload is stored in `thinkingSignature` so it can be passed back to the API for multi-turn continuity.'
        ),
    ] = None
    thinking: str
    thinkingSignature: str | None = None
    type: Literal['thinking']


class ToolResultMessage(AllowExtra):
    content: list[TextContent | ImageContent]
    details: Any | None = None
    isError: bool
    role: Literal['toolResult']
    timestamp: float
    toolCallId: str
    toolName: str


class Cost(AllowExtra):
    cacheRead: float
    cacheWrite: float
    input: float
    output: float
    total: float


class Usage(AllowExtra):
    cacheRead: float
    cacheWrite: float
    cost: Cost
    input: float
    output: float
    totalTokens: float


class UserMessage(AllowExtra):
    content: str | list[TextContent | ImageContent]
    role: Literal['user']
    timestamp: float


class AgentStartEvent(AllowExtra):
    """
    Events emitted by the Agent for UI updates.

    `agent_end` is the last event emitted for a run, but awaited `Agent.subscribe()` listeners for that event are still part of run settlement. The agent becomes idle only after those listeners finish.
    """

    type: Literal['agent_start']


class TurnStartEvent(AllowExtra):
    """
    Events emitted by the Agent for UI updates.

    `agent_end` is the last event emitted for a run, but awaited `Agent.subscribe()` listeners for that event are still part of run settlement. The agent becomes idle only after those listeners finish.
    """

    type: Literal['turn_start']


class ToolExecutionStartEvent(AllowExtra):
    """
    Events emitted by the Agent for UI updates.

    `agent_end` is the last event emitted for a run, but awaited `Agent.subscribe()` listeners for that event are still part of run settlement. The agent becomes idle only after those listeners finish.
    """

    args: Any
    toolCallId: str
    toolName: str
    type: Literal['tool_execution_start']


class ToolExecutionUpdateEvent(AllowExtra):
    """
    Events emitted by the Agent for UI updates.

    `agent_end` is the last event emitted for a run, but awaited `Agent.subscribe()` listeners for that event are still part of run settlement. The agent becomes idle only after those listeners finish.
    """

    args: Any
    partialResult: Any
    toolCallId: str
    toolName: str
    type: Literal['tool_execution_update']


class ToolExecutionEndEvent(AllowExtra):
    """
    Events emitted by the Agent for UI updates.

    `agent_end` is the last event emitted for a run, but awaited `Agent.subscribe()` listeners for that event are still part of run settlement. The agent becomes idle only after those listeners finish.
    """

    isError: bool
    result: Any
    toolCallId: str
    toolName: str
    type: Literal['tool_execution_end']


class Record3Cstring2Cany3E(AllowExtra):
    pass


class ThinkingLevelChangedEvent(AllowExtra):
    """
    Session-specific events that extend the core AgentEvent
    """

    level: Annotated[
        Literal['off', 'minimal', 'low', 'medium', 'high', 'xhigh'],
        Field(
            description='Thinking/reasoning level for models that support it. Note: "xhigh" is only supported by selected model families. Use model thinking-level metadata from'
        ),
    ]
    type: Literal['thinking_level_changed']


class CompactionEndEvent(AllowExtra):
    """
    Session-specific events that extend the core AgentEvent
    """

    aborted: bool
    errorMessage: str | None = None
    reason: Literal['manual', 'threshold', 'overflow']
    result: CompactionResult | None = None
    type: Literal['compaction_end']
    willRetry: bool


class AssistantMessageDiagnostic(AllowExtra):
    details: dict[str, Any] | None = None
    error: DiagnosticErrorInfo | None = None
    timestamp: float
    type: str


class ToolCall(AllowExtra):
    arguments: Record3Cstring2Cany3E
    id: str
    name: str
    thoughtSignature: str | None = None
    type: Literal['toolCall']


class AssistantMessage(AllowExtra):
    api: (
        str
        | Literal[
            'openai-completions',
            'mistral-conversations',
            'openai-responses',
            'azure-openai-responses',
            'openai-codex-responses',
            'anthropic-messages',
            'bedrock-converse-stream',
            'google-generative-ai',
            'google-vertex',
        ]
    )
    content: list[TextContent | ThinkingContent | ToolCall]
    diagnostics: list[AssistantMessageDiagnostic] | None = None
    errorMessage: str | None = None
    model: str
    provider: (
        str
        | Literal[
            'amazon-bedrock',
            'ant-ling',
            'anthropic',
            'google',
            'google-vertex',
            'openai',
            'azure-openai-responses',
            'openai-codex',
            'nvidia',
            'deepseek',
            'github-copilot',
            'xai',
            'groq',
            'cerebras',
            'openrouter',
            'vercel-ai-gateway',
            'zai',
            'zai-coding-cn',
            'mistral',
            'minimax',
            'minimax-cn',
            'moonshotai',
            'moonshotai-cn',
            'huggingface',
            'fireworks',
            'together',
            'opencode',
            'opencode-go',
            'kimi-coding',
            'cloudflare-workers-ai',
            'cloudflare-ai-gateway',
            'xiaomi',
            'xiaomi-token-plan-cn',
            'xiaomi-token-plan-ams',
            'xiaomi-token-plan-sgp',
        ]
    )
    responseId: str | None = None
    responseModel: str | None = None
    role: Literal['assistant']
    stopReason: Literal['stop', 'length', 'toolUse', 'error', 'aborted']
    timestamp: float
    usage: Usage


class AssistantMessageStart(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    partial: AssistantMessage
    type: Literal['start']


class AssistantMessageTextStart(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    contentIndex: float
    partial: AssistantMessage
    type: Literal['text_start']


class AssistantMessageTextDelta(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    contentIndex: float
    delta: str
    partial: AssistantMessage
    type: Literal['text_delta']


class AssistantMessageTextEnd(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    content: str
    contentIndex: float
    partial: AssistantMessage
    type: Literal['text_end']


class AssistantMessageThinkingStart(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    contentIndex: float
    partial: AssistantMessage
    type: Literal['thinking_start']


class AssistantMessageThinkingDelta(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    contentIndex: float
    delta: str
    partial: AssistantMessage
    type: Literal['thinking_delta']


class AssistantMessageThinkingEnd(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    content: str
    contentIndex: float
    partial: AssistantMessage
    type: Literal['thinking_end']


class AssistantMessageToolcallStart(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    contentIndex: float
    partial: AssistantMessage
    type: Literal['toolcall_start']


class AssistantMessageToolcallDelta(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    contentIndex: float
    delta: str
    partial: AssistantMessage
    type: Literal['toolcall_delta']


class AssistantMessageToolcallEnd(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    contentIndex: float
    partial: AssistantMessage
    toolCall: ToolCall
    type: Literal['toolcall_end']


class AssistantMessageDone(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    message: AssistantMessage
    reason: Literal['stop', 'length', 'toolUse']
    type: Literal['done']


class AssistantMessageError(AllowExtra):
    """
    Event protocol for AssistantMessageEventStream.

    Streams should emit `start` before partial updates, then terminate with either:
    - `done` carrying the final successful AssistantMessage, or
    - `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.
    """

    error: AssistantMessage
    reason: Literal['error', 'aborted']
    type: Literal['error']


class AgentEndEvent(AllowExtra):
    """
    Session-specific events that extend the core AgentEvent
    """

    messages: list[UserMessage | AssistantMessage | ToolResultMessage]
    type: Literal['agent_end']
    willRetry: bool


class TurnEndEvent(AllowExtra):
    """
    Events emitted by the Agent for UI updates.

    `agent_end` is the last event emitted for a run, but awaited `Agent.subscribe()` listeners for that event are still part of run settlement. The agent becomes idle only after those listeners finish.
    """

    message: Annotated[
        UserMessage | AssistantMessage | ToolResultMessage,
        Field(
            description='AgentMessage: Union of LLM messages + custom messages. This abstraction allows apps to add custom message types while maintaining type safety and compatibility with the base LLM messages.'
        ),
    ]
    toolResults: list[ToolResultMessage]
    type: Literal['turn_end']


class MessageStartEvent(AllowExtra):
    """
    Events emitted by the Agent for UI updates.

    `agent_end` is the last event emitted for a run, but awaited `Agent.subscribe()` listeners for that event are still part of run settlement. The agent becomes idle only after those listeners finish.
    """

    message: Annotated[
        UserMessage | AssistantMessage | ToolResultMessage,
        Field(
            description='AgentMessage: Union of LLM messages + custom messages. This abstraction allows apps to add custom message types while maintaining type safety and compatibility with the base LLM messages.'
        ),
    ]
    type: Literal['message_start']


class MessageUpdateEvent(AllowExtra):
    """
    Events emitted by the Agent for UI updates.

    `agent_end` is the last event emitted for a run, but awaited `Agent.subscribe()` listeners for that event are still part of run settlement. The agent becomes idle only after those listeners finish.
    """

    assistantMessageEvent: Annotated[
        AssistantMessageStart
        | AssistantMessageTextStart
        | AssistantMessageTextDelta
        | AssistantMessageTextEnd
        | AssistantMessageThinkingStart
        | AssistantMessageThinkingDelta
        | AssistantMessageThinkingEnd
        | AssistantMessageToolcallStart
        | AssistantMessageToolcallDelta
        | AssistantMessageToolcallEnd
        | AssistantMessageDone
        | AssistantMessageError,
        Field(
            description='Event protocol for AssistantMessageEventStream.\n\nStreams should emit `start` before partial updates, then terminate with either:\n- `done` carrying the final successful AssistantMessage, or\n- `error` carrying the final AssistantMessage with stopReason "error" or "aborted"   and errorMessage.'
        ),
    ]
    message: Annotated[
        UserMessage | AssistantMessage | ToolResultMessage,
        Field(
            description='AgentMessage: Union of LLM messages + custom messages. This abstraction allows apps to add custom message types while maintaining type safety and compatibility with the base LLM messages.'
        ),
    ]
    type: Literal['message_update']


class MessageEndEvent(AllowExtra):
    """
    Events emitted by the Agent for UI updates.

    `agent_end` is the last event emitted for a run, but awaited `Agent.subscribe()` listeners for that event are still part of run settlement. The agent becomes idle only after those listeners finish.
    """

    message: Annotated[
        UserMessage | AssistantMessage | ToolResultMessage,
        Field(
            description='AgentMessage: Union of LLM messages + custom messages. This abstraction allows apps to add custom message types while maintaining type safety and compatibility with the base LLM messages.'
        ),
    ]
    type: Literal['message_end']


class AgentSessionEvent(
    RootModel[
        AgentEndEvent
        | QueueUpdateEvent
        | CompactionStartEvent
        | SessionInfoChangedEvent
        | ThinkingLevelChangedEvent
        | CompactionEndEvent
        | AutoRetryStartEvent
        | AutoRetryEndEvent
        | AgentStartEvent
        | TurnStartEvent
        | TurnEndEvent
        | MessageStartEvent
        | MessageUpdateEvent
        | MessageEndEvent
        | ToolExecutionStartEvent
        | ToolExecutionUpdateEvent
        | ToolExecutionEndEvent
    ]
):
    root: Annotated[
        AgentEndEvent
        | QueueUpdateEvent
        | CompactionStartEvent
        | SessionInfoChangedEvent
        | ThinkingLevelChangedEvent
        | CompactionEndEvent
        | AutoRetryStartEvent
        | AutoRetryEndEvent
        | AgentStartEvent
        | TurnStartEvent
        | TurnEndEvent
        | MessageStartEvent
        | MessageUpdateEvent
        | MessageEndEvent
        | ToolExecutionStartEvent
        | ToolExecutionUpdateEvent
        | ToolExecutionEndEvent,
        Field(description='Session-specific events that extend the core AgentEvent'),
    ]
